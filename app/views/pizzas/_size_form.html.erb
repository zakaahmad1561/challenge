<div class="nested-form-wrapper" data-new-record="<%= form.object.new_record? %>">
  <%= form.label :name %>
  <%= select_tag :status, options_for_select(Size.names) %>

  
  <%= form.label :price %>
  <%= form.number_field :price, step: 0.01, min: 0.0%>

  <button type="button" data-action="nested-form#remove">Remove todo</button>

  <%= form.hidden_field :_destroy %>
</div>